import AppShell from "@/components/AppShell";

export default function LeaderboardLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Leaderboard">{children}</AppShell>;
}
