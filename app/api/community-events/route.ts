import { NextRequest, NextResponse } from "next/server";
import { createServerSupabaseClient } from "@/lib/supabase/server";
export async function GET(){const supabase=await createServerSupabaseClient();const {data,error}=await supabase.from("community_events").select("*, community_event_rsvps(*)").order("starts_at",{ascending:true});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({events:data||[]});}
export async function POST(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();if(!String(body.title||"").trim()||!body.startsAt)return NextResponse.json({error:"Title and start time are required."},{status:422});const {data,error}=await supabase.from("community_events").insert({created_by:auth.user.id,title:String(body.title).trim(),description:body.description||null,event_type:body.eventType||"community",location:body.location||null,starts_at:body.startsAt,ends_at:body.endsAt||null,visibility:"public"}).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,event:data},{status:201});}
