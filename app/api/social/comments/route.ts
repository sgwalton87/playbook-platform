import { NextRequest,NextResponse } from "next/server";
import { createServerSupabaseClient } from "@/lib/supabase/server";
async function context(){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();return {supabase,user:auth.user};}
export async function POST(req:NextRequest){const {supabase,user}=await context();if(!user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();if(!body.postId||!String(body.body||"").trim())return NextResponse.json({error:"Post and comment are required."},{status:422});const {data,error}=await supabase.from("feed_post_comments").insert({post_id:body.postId,user_id:user.id,body:String(body.body).trim()}).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,comment:data},{status:201});}
export async function PATCH(req:NextRequest){const {supabase,user}=await context();if(!user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();if(!body.commentId||!String(body.body||"").trim())return NextResponse.json({error:"Comment and body are required."},{status:422});const {data,error}=await supabase.from("feed_post_comments").update({body:String(body.body).trim()}).eq("id",body.commentId).eq("user_id",user.id).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,comment:data});}
export async function DELETE(req:NextRequest){const {supabase,user}=await context();if(!user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();if(!body.commentId)return NextResponse.json({error:"Comment is required."},{status:422});const {error}=await supabase.from("feed_post_comments").delete().eq("id",body.commentId).eq("user_id",user.id);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
