import { NextRequest,NextResponse } from "next/server";
import { createServerSupabaseClient } from "@/lib/supabase/server";
type Profile={display_name?:string|null;role?:string|null;organization?:string|null;expertise?:string[]|null};
export async function GET(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:"Sign in required."},{status:401});const q=req.nextUrl.searchParams.get("q")?.toLowerCase()||"";const role=req.nextUrl.searchParams.get("role")||"";let query=supabase.from("support_directory_profiles").select("user_id,display_name,role,organization,expertise").eq("searchable",true).order("display_name");if(role)query=query.eq("role",role);const {data,error}=await query;if(error)return NextResponse.json({error:error.message},{status:400});const filtered=((data||[]) as Profile[]).filter(person=>!q||[person.display_name,person.role,person.organization,...(person.expertise||[])].join(" ").toLowerCase().includes(q));return NextResponse.json({mentors:filtered});}
export async function POST(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();const {data,error}=await supabase.from("support_directory_profiles").upsert({user_id:auth.user.id,role:body.role,display_name:body.displayName,organization:body.organization||null,expertise:body.expertise||[],searchable:body.searchable??true},{onConflict:"user_id"}).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,profile:data});}
