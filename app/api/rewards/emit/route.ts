import { NextRequest,NextResponse } from "next/server";
import { buildCoinLedgerEntry,buildRewardEvent,type RewardEventType } from "@/lib/reward-events";
import { createServerSupabaseClient } from "@/lib/supabase/server";
export async function POST(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:"Sign in required."},{status:401});const {data:admin}=await supabase.rpc("is_platform_admin",{p_user:auth.user.id});if(admin!==true)return NextResponse.json({error:"Administrator access required."},{status:403});const body=await req.json();const event=buildRewardEvent({scholarId:body.scholarId,eventType:body.eventType as RewardEventType,sourceId:body.sourceId,payload:body.payload||{}});const ledger=buildCoinLedgerEntry({scholarId:body.scholarId,eventType:body.eventType as RewardEventType,sourceId:body.sourceId});const {data,error}=await supabase.rpc("emit_reward_event",{p_event:event,p_ledger:ledger});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,result:data},{status:201});}
