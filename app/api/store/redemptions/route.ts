import { NextRequest,NextResponse } from "next/server";
import { createServerSupabaseClient } from "@/lib/supabase/server";
export async function POST(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:"Sign in required."},{status:401});const body=await req.json();if(!body.productId)return NextResponse.json({error:"Product is required."},{status:422});const {data,error}=await supabase.rpc("redeem_store_reward",{p_product_id:body.productId,p_shipping_payload:body.shippingPayload||{}});return error?NextResponse.json({error:"Redemption was unavailable or the balance was insufficient."},{status:400}):NextResponse.json({ok:true,redemption:data},{status:201});}
