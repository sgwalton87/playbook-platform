import type { Metadata } from "next";

export const metadata:Metadata={title:"Scholarship Finder | Playbook",description:"Discover human-reviewed scholarship opportunities and start a governed Application Workspace."};
export default function ScholarshipsLayout({children}:{children:React.ReactNode}){return children;}
