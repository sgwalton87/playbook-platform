import AppShell from "@/components/AppShell";

export default function ConnectionsLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Connections">{children}</AppShell>;
}
