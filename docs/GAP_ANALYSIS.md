# Gap Analysis

Integration Matrix files requested at `docs/INTEGRATION_MATRIX.md`, `docs/SPRINT_BACKLOG.md`, `docs/VERIFICATION_STANDARD.md`, and `docs/CODEX_ENGINEERING_CONTRACT.md` were not present at repository root during verification. Rows below use the user-requested dependency chain as the matrix surrogate and mark statuses only from verified files/checks.

## Playbook Record
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## Scholar Record
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## Trust Layer
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## Graph
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## Opportunity
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## Compass
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.

## UI
- Current status: Partially implemented.
- Why not green: repository-wide lint fails and production build fails; row-specific runtime integration was not proven.
- Verification evidence: matching paths are cataloged in `docs/REPOSITORY_INDEX.md`; checks are cataloged in `docs/IMPLEMENTATION_STATUS.md`.
- Estimated scope: audit row-specific tests, fix lint/build blockers, then run route-level verification.
- Risks: duplicate/competing implementations listed in duplicate report may obscure canonical ownership.
- Files likely involved: see matching catalog entries in `docs/REPOSITORY_INDEX.md`.
- Acceptance criteria: row has canonical model/service/repository/API/UI path, tests pass, TypeScript passes, lint passes, and build passes.
