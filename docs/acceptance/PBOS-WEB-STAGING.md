# PBOS Playbook Web Staging

PBOS deploys this mission only after independent validation passes on the exact pull-request revision. The Vercel project must be bound to `sgwalton87/playbook-platform`; required configuration is inspected by name and scope without logging values. Production deployment is explicitly excluded. Desktop and responsive-mobile browser journeys run against the resulting preview URL.
