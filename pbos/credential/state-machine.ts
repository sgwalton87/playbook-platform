import type { CredentialLifecycleState, CredentialState } from "./contracts";
import { CredentialError, credentialFailure } from "./errors";
const next: Record<CredentialState, CredentialState[]> = { CREATED: ["EVIDENCE_ATTACHED"], EVIDENCE_ATTACHED: ["ISSUER_VALIDATION"], ISSUER_VALIDATION: ["APPROVAL_REVIEW"], APPROVAL_REVIEW: ["ISSUED"], ISSUED: ["VERIFIED"], VERIFIED: ["SHARED", "EXPIRED", "REVOKED"], SHARED: ["EXPIRED", "REVOKED", "ARCHIVED"], EXPIRED: ["ARCHIVED"], REVOKED: ["ARCHIVED"], ARCHIVED: [] };
export function transitionCredential(state: CredentialLifecycleState, to: CredentialState, transitionedAt: string, authorityIdentity: string, evidenceReferences: string[]): CredentialLifecycleState { if (!next[state.currentState].includes(to)) throw new CredentialError([credentialFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (["ISSUED", "VERIFIED", "SHARED", "EXPIRED", "REVOKED", "ARCHIVED"].includes(to) && (!authorityIdentity || !evidenceReferences.length)) throw new CredentialError([credentialFailure("GOVERNANCE_BYPASS", "Governed credential transitions require authority identity and evidence.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, authorityIdentity, evidenceReferences: [...evidenceReferences].sort() }] }; }
