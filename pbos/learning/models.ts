import { digestValue } from "../context";
import type { Competency, CompetencyDraft, LearnerEvidence, LearningProvenance, Skill, SkillDraft } from "./contracts";

export function provenance(learnerId: string, evidenceIds: string[], evidence: LearnerEvidence[], contextDigest: string): LearningProvenance { const selected = evidence.filter((item) => evidenceIds.includes(item.evidenceId)); return { learnerId, evidenceIds: [...evidenceIds].sort(), sourceReferences: selected.map(({ sourceReference }) => sourceReference).sort(), owners: [...new Set(selected.map(({ owner }) => owner))].sort(), runtimeContextDigest: contextDigest }; }
export function createCompetencies(drafts: CompetencyDraft[], learnerId: string, evidence: LearnerEvidence[], contextDigest: string): Competency[] { return drafts.map((draft) => { const body = { ...draft, evidenceRequirements: [...draft.evidenceRequirements].sort(), relatedSkillNames: [...draft.relatedSkillNames].sort(), masteryIndicators: [...draft.masteryIndicators].sort(), limitations: [...draft.limitations].sort(), sourceEvidenceIds: [...draft.sourceEvidenceIds].sort(), provenance: provenance(learnerId, draft.sourceEvidenceIds, evidence, contextDigest), developmental: true as const }; return { competencyId: `PBOS-LEARN-COMP-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }).sort((a, b) => a.competencyId.localeCompare(b.competencyId)); }
export function createSkills(drafts: SkillDraft[], learnerId: string, evidence: LearnerEvidence[], contextDigest: string): Skill[] { return drafts.map((draft) => { const body = { ...draft, evidenceIds: [...draft.evidenceIds].sort(), practiceOpportunities: [...draft.practiceOpportunities].sort(), progressionIndicators: [...draft.progressionIndicators].sort(), limitations: [...draft.limitations].sort(), provenance: provenance(learnerId, draft.evidenceIds, evidence, contextDigest) }; return { skillId: `PBOS-LEARN-SKILL-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }).sort((a, b) => a.skillId.localeCompare(b.skillId)); }
