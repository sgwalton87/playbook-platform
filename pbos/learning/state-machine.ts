import type { LearningApproval, LearningMachineState, LearningState } from "./contracts";
import { LearningError, learningFailure } from "./errors";
const next: Record<LearningState, LearningState[]> = { OBSERVING: ["ASSESSING_CONTEXT"], ASSESSING_CONTEXT: ["IDENTIFYING_GOALS"], IDENTIFYING_GOALS: ["MAPPING_COMPETENCIES"], MAPPING_COMPETENCIES: ["BUILDING_PATHWAY"], BUILDING_PATHWAY: ["TRACKING_PROGRESS"], TRACKING_PROGRESS: ["REFLECTING"], REFLECTING: ["RECOMMENDING_NEXT_STEPS"], RECOMMENDING_NEXT_STEPS: ["MENTOR_REVIEW"], MENTOR_REVIEW: ["ARCHIVED"], ARCHIVED: [] };
export function transitionLearning(state: LearningMachineState, to: LearningState, transitionedAt: string, approval: LearningApproval): LearningMachineState { if (!next[state.currentState].includes(to)) throw new LearningError([learningFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (to === "ARCHIVED" && (approval.status !== "approved" || !approval.approvalIdentifier || !approval.evidenceReferences.length)) throw new LearningError([learningFailure("GOVERNANCE_BYPASS", "Archival after mentor review requires recorded human approval.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, approvalIdentifier: approval.approvalIdentifier, evidenceReferences: [...approval.evidenceReferences].sort() }] }; }
