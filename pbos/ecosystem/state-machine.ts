import type { EcosystemLifecycleState, EcosystemState } from "./contracts";
import { EcosystemError, ecosystemFailure } from "./errors";
const next: Record<EcosystemState, EcosystemState[]> = { DISCOVERING: ["VERIFYING"], VERIFYING: ["MAPPING"], MAPPING: ["CONNECTING"], CONNECTING: ["SUPPORTING"], SUPPORTING: ["REVIEWING"], REVIEWING: ["ARCHIVED"], ARCHIVED: [] };
export function transitionEcosystem(state: EcosystemLifecycleState, to: EcosystemState, transitionedAt: string, authorityIdentity: string, evidenceReferences: string[]): EcosystemLifecycleState { if (!next[state.currentState].includes(to)) throw new EcosystemError([ecosystemFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (["CONNECTING", "SUPPORTING", "REVIEWING", "ARCHIVED"].includes(to) && (!authorityIdentity || !evidenceReferences.length)) throw new EcosystemError([ecosystemFailure("GOVERNANCE_BYPASS", "Human relationship stages require authority identity and evidence.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, authorityIdentity, evidenceReferences: [...evidenceReferences].sort() }] }; }
