import type { MobilityLifecycle, MobilityState } from "./contracts";
import { MobilityError, mobilityFailure } from "./errors";
const next: Record<MobilityState, MobilityState[]> = { CREATED: ["EXPLORING"], EXPLORING: ["PLANNING"], PLANNING: ["PREPARING"], PREPARING: ["CONNECTING"], CONNECTING: ["TRANSITIONING"], TRANSITIONING: ["REFLECTING"], REFLECTING: ["ARCHIVED"], ARCHIVED: [] };
export function transitionMobility(state: MobilityLifecycle, to: MobilityState, transitionedAt: string, actorIdentity: string, evidenceReferences: string[]): MobilityLifecycle { if (!next[state.currentState].includes(to)) throw new MobilityError([mobilityFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (!actorIdentity || !evidenceReferences.length || Number.isNaN(Date.parse(transitionedAt))) throw new MobilityError([mobilityFailure("GOVERNANCE_BYPASS", "Transitions require a human actor, evidence, and valid timestamp.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, actorIdentity, evidenceReferences: [...evidenceReferences].sort() }] }; }
