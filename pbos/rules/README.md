# PBOS Engine Rules

Rules in this directory constrain engine behavior without replacing the handbook.
PBOS Engine v1 uses the handbook hierarchy in `pbos/config/pbos.config.json` and the structured gate definitions in `pbos/gates/`.
