import type{AccessLifecycle,AccessState}from"./contracts";import{AccessError,accessFailure}from"./errors";const n:Record<AccessState,AccessState[]>={CREATED:["VERIFYING"],VERIFYING:["VERIFIED"],VERIFIED:["ACTIVE"],ACTIVE:["RESTRICTED","SUSPENDED"],RESTRICTED:["ACTIVE","SUSPENDED","REVOKED"],SUSPENDED:["REVOKED"],REVOKED:["ARCHIVED"],ARCHIVED:[]};export function transitionAccess(s:AccessLifecycle,to:AccessState,transitionedAt:string,actorIdentity:string,evidenceReferences:string[]):AccessLifecycle{if(!n[s.currentState].includes(to)||!actorIdentity||!evidenceReferences.length||Number.isNaN(Date.parse(transitionedAt)))throw new AccessError([accessFailure("INVALID_TRANSITION","Invalid or unauthorized access transition")]);return{currentState:to,transitions:[...s.transitions,{from:s.currentState,to,transitionedAt,actorIdentity,evidenceReferences}]}}
