import type { AcademicLifecycleState, AcademicState } from "./contracts";
import { AcademicError, academicFailure } from "./errors";
const next: Record<AcademicState, AcademicState[]> = { CREATED: ["EXPLORING"], EXPLORING: ["PLANNING"], PLANNING: ["TRACKING"], TRACKING: ["REVIEWING"], REVIEWING: ["PATHWAY_READY"], PATHWAY_READY: ["TRANSITIONING", "COMPLETED"], TRANSITIONING: ["COMPLETED", "LIFELONG_LEARNING"], COMPLETED: ["LIFELONG_LEARNING"], LIFELONG_LEARNING: ["ARCHIVED"], ARCHIVED: [] };
export function transitionAcademic(state: AcademicLifecycleState, to: AcademicState, transitionedAt: string, authorityIdentity: string, evidenceReferences: string[]): AcademicLifecycleState { if (!next[state.currentState].includes(to)) throw new AcademicError([academicFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (["REVIEWING", "PATHWAY_READY", "TRANSITIONING", "COMPLETED", "ARCHIVED"].includes(to) && (!authorityIdentity || !evidenceReferences.length)) throw new AcademicError([academicFailure("GOVERNANCE_BYPASS", "Review, pathway, transition, completion, and archival require human authority and evidence.")]); if (Number.isNaN(Date.parse(transitionedAt))) throw new AcademicError([academicFailure("MISSING_EVIDENCE", "Lifecycle transitions require a valid timestamp.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, authorityIdentity, evidenceReferences: [...evidenceReferences].sort() }] }; }
