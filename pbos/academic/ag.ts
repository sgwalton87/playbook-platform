import { digestValue } from "../context";
import type { AGRequirementDraft, AGTrackingRecord, CourseworkRecord } from "./contracts";
export function createAGTracking(drafts: AGRequirementDraft[], coursework: CourseworkRecord[]): AGTrackingRecord[] { return drafts.map((draft) => { const completedCourseNames = draft.requiredCourseNames.filter((name) => coursework.some((course) => course.courseName === name && course.status === "COMPLETED" && course.recordClass === "VERIFIED_INSTITUTIONAL_RECORD")).sort(); const missingCourseNames = draft.requiredCourseNames.filter((name) => !completedCourseNames.includes(name)).sort(); const body = { ...draft, requiredCourseNames: [...draft.requiredCourseNames].sort(), completedCourseNames, missingCourseNames, evidenceReferences: [...draft.evidenceReferences].sort(), expandableStandard: true as const }; return { agRecordId: `PBOS-ACAD-AG-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }).sort((a, b) => a.agRecordId.localeCompare(b.agRecordId)); }
