import { digestValue } from "../context";
import type { KnowledgePrecedent, PrecedentInput } from "./governed-contracts";
import { KnowledgeError, knowledgeFailure } from "./errors";
export function createPrecedents(inputs: PrecedentInput[], contextDigest: string): KnowledgePrecedent[] { return inputs.map((input) => { if (!input.evidenceReferences.length || !input.sourceRecordIds.length || !input.limitations.length || !input.applicabilityConditions.length) throw new KnowledgeError([knowledgeFailure("MISSING_PROVENANCE", "Precedents require history, evidence, limitations, and applicability conditions.")]); const provenance = { sourceRecordIds: [...input.sourceRecordIds].sort(), evidenceReferences: [...input.evidenceReferences].sort(), owners: [] as string[], sourceContextDigest: contextDigest }; const body = { ...input, evidenceReferences: [...input.evidenceReferences].sort(), sourceRecordIds: [...input.sourceRecordIds].sort(), distinction: "HISTORICAL_NOT_PRESCRIPTIVE" as const, provenance }; return { precedentId: `PBOS-KNOW-PRE-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }).sort((a, b) => a.precedentId.localeCompare(b.precedentId)); }
