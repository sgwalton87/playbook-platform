import type { KnowledgeApproval, KnowledgeMachineState, KnowledgeState } from "./governed-contracts";
import { KnowledgeError, knowledgeFailure } from "./errors";
const next: Record<KnowledgeState, KnowledgeState[]> = { OBSERVING: ["INGESTING"], INGESTING: ["VALIDATING"], VALIDATING: ["CONNECTING"], CONNECTING: ["INDEXING"], INDEXING: ["RETRIEVING"], RETRIEVING: ["REPORTING"], REPORTING: ["GOVERNANCE_REVIEW"], GOVERNANCE_REVIEW: ["ARCHIVED"], ARCHIVED: [] };
export function transitionKnowledge(state: KnowledgeMachineState, to: KnowledgeState, transitionedAt: string, approval: KnowledgeApproval): KnowledgeMachineState { if (!next[state.currentState].includes(to)) throw new KnowledgeError([knowledgeFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (to === "ARCHIVED" && (approval.status !== "approved" || !approval.approvalIdentifier || !approval.evidenceReferences.length)) throw new KnowledgeError([knowledgeFailure("GOVERNANCE_BYPASS", "Archival requires recorded human governance approval.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, approvalIdentifier: approval.approvalIdentifier, evidenceReferences: [...approval.evidenceReferences].sort() }] }; }
