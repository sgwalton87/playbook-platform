import { digestValue } from "../context";
import type { PathwayComparison, SimulationAssumption, SimulationPathway } from "./contracts";
export function comparePathways(pathways: SimulationPathway[], assumptions: SimulationAssumption[], constraints: string[]): PathwayComparison { const ordered = [...pathways].sort((a, b) => a.pathwayId.localeCompare(b.pathwayId)); const evidenceUsed = [...new Set(ordered.flatMap((pathway) => pathway.supportingEvidence))].sort(); const body = { pathwayIds: ordered.map(({ pathwayId }) => pathwayId), comparisonMethod: "Side-by-side descriptive comparison of disclosed resources, risks, unknowns, assumptions, and constraints; no weighted winner is calculated.", dimensions: ordered.map((pathway) => ({ pathwayId: pathway.pathwayId, tradeoffs: [...pathway.unknownFactors].sort(), risks: [...pathway.risks].sort(), resourceCount: pathway.requiredResources.length, assumptionCount: assumptions.length, constraintCount: constraints.length })), evidenceUsed, limitations: ["Counts do not measure desirability, probability, cost, or success.", "Human governance must interpret tradeoffs and select any path."], confidence: evidenceUsed.length > 2 ? "HIGH" as const : "MEDIUM" as const, advisoryOnly: true as const, selectedPathway: null }; return { comparisonId: `PBOS-SIM-CMP-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }
