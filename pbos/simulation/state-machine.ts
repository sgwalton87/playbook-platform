import type { SimulationApproval, SimulationMachineState, SimulationState } from "./contracts";
import { SimulationError, simulationFailure } from "./errors";
const next: Record<SimulationState, SimulationState[]> = { OBSERVING: ["DEFINING_QUESTION"], DEFINING_QUESTION: ["GATHERING_EVIDENCE"], GATHERING_EVIDENCE: ["MODELING_ASSUMPTIONS"], MODELING_ASSUMPTIONS: ["GENERATING_SCENARIOS"], GENERATING_SCENARIOS: ["COMPARING_PATHWAYS"], COMPARING_PATHWAYS: ["REPORTING"], REPORTING: ["GOVERNANCE_REVIEW"], GOVERNANCE_REVIEW: ["ARCHIVED"], ARCHIVED: [] };
export function transitionSimulation(state: SimulationMachineState, to: SimulationState, transitionedAt: string, approval: SimulationApproval): SimulationMachineState { if (!next[state.currentState].includes(to)) throw new SimulationError([simulationFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (to === "ARCHIVED" && (approval.status !== "approved" || !approval.approvalIdentifier || !approval.evidenceReferences.length)) throw new SimulationError([simulationFailure("GOVERNANCE_BYPASS", "Archival requires recorded human governance approval.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, approvalIdentifier: approval.approvalIdentifier, evidenceReferences: [...approval.evidenceReferences].sort() }] }; }
