import type{CompassLifecycleState,CompassState}from"./contracts";import{CompassError,compassFailure}from"./errors";const n:Record<CompassState,CompassState[]>={CREATED:["UNDERSTANDING"],UNDERSTANDING:["MAPPING"],MAPPING:["IDENTIFYING_OPTIONS"],IDENTIFYING_OPTIONS:["PRESENTING_GUIDANCE"],PRESENTING_GUIDANCE:["USER_REVIEW"],USER_REVIEW:["ACTION_SELECTED","REFLECTION"],ACTION_SELECTED:["REFLECTION"],REFLECTION:["ARCHIVED"],ARCHIVED:[]};export function transitionCompass(s:CompassLifecycleState,to:CompassState,at:string,authority:string,evidence:string[]){if(!n[s.currentState].includes(to))throw new CompassError([compassFailure("INVALID_TRANSITION",`Cannot transition from ${s.currentState} to ${to}.`)]);if(["USER_REVIEW","ACTION_SELECTED","REFLECTION","ARCHIVED"].includes(to)&&(!authority||!evidence.length))throw new CompassError([compassFailure("GOVERNANCE_BYPASS","User stages require person authority and evidence.")]);return{currentState:to,transitions:[...s.transitions,{from:s.currentState,to,transitionedAt:at,authorityIdentity:authority,evidenceReferences:[...evidence].sort()}]}}
