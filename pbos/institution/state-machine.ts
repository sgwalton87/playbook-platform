import type { InstitutionLifecycle, InstitutionState } from "./contracts"; import { InstitutionError, institutionFailure } from "./errors";
const next: Record<InstitutionState, InstitutionState[]> = { CREATED: ["VERIFYING"], VERIFYING: ["VERIFIED"], VERIFIED: ["ACTIVE"], ACTIVE: ["PROGRAM_OPERATING", "SUSPENDED"], PROGRAM_OPERATING: ["REVIEWING", "SUSPENDED"], REVIEWING: ["ACTIVE", "SUSPENDED"], SUSPENDED: ["ARCHIVED"], ARCHIVED: [] };
export function transitionInstitution(state: InstitutionLifecycle, to: InstitutionState, transitionedAt: string, actorIdentity: string, evidenceReferences: string[]): InstitutionLifecycle { if (!next[state.currentState].includes(to)) throw new InstitutionError([institutionFailure("INVALID_TRANSITION", "Invalid institution transition.")]); if (!actorIdentity || !evidenceReferences.length || Number.isNaN(Date.parse(transitionedAt))) throw new InstitutionError([institutionFailure("GOVERNANCE_BYPASS", "Transitions require human authority and evidence.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, actorIdentity, evidenceReferences }] }; }
