import type { OpportunityLifecycleState, OpportunityState } from "./contracts";
import { OpportunityError, opportunityFailure } from "./errors";
const next: Record<OpportunityState, OpportunityState[]> = { DISCOVERING: ["VALIDATING_SOURCE"], VALIDATING_SOURCE: ["ANALYZING_REQUIREMENTS"], ANALYZING_REQUIREMENTS: ["ASSESSING_ALIGNMENT"], ASSESSING_ALIGNMENT: ["PRESENTING_OPTIONS"], PRESENTING_OPTIONS: ["HUMAN_SELECTION"], HUMAN_SELECTION: ["APPLICATION_SUPPORT"], APPLICATION_SUPPORT: ["OUTCOME_RECORDING"], OUTCOME_RECORDING: ["ARCHIVED"], ARCHIVED: [] };
export function transitionOpportunity(state: OpportunityLifecycleState, to: OpportunityState, transitionedAt: string, authorityIdentity: string, evidenceReferences: string[]): OpportunityLifecycleState { if (!next[state.currentState].includes(to)) throw new OpportunityError([opportunityFailure("INVALID_TRANSITION", `Cannot transition from ${state.currentState} to ${to}.`)]); if (["HUMAN_SELECTION", "APPLICATION_SUPPORT", "OUTCOME_RECORDING", "ARCHIVED"].includes(to) && (!authorityIdentity || !evidenceReferences.length)) throw new OpportunityError([opportunityFailure("GOVERNANCE_BYPASS", "Human-controlled opportunity stages require authority identity and evidence.")]); return { currentState: to, transitions: [...state.transitions, { from: state.currentState, to, transitionedAt, authorityIdentity, evidenceReferences: [...evidenceReferences].sort() }] }; }
