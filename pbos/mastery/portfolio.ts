import { digestValue } from "../context";
import type { GovernedPortfolio, MasteryEvidence, PortfolioDraft } from "./contracts";
import { provenance } from "./models";
export function createPortfolio(draft: PortfolioDraft, learnerId: string, evidence: MasteryEvidence[], contextDigest: string, createdAt: string): GovernedPortfolio { const evidenceIds = [...new Set([...draft.artifactEvidenceIds, ...draft.reflectionEvidenceIds])].sort(); const body = { ...draft, artifactEvidenceIds: [...draft.artifactEvidenceIds].sort(), competencyNames: [...draft.competencyNames].sort(), achievementDescriptions: [...draft.achievementDescriptions].sort(), reflectionEvidenceIds: [...draft.reflectionEvidenceIds].sort(), validationIds: [...draft.validationIds].sort(), owner: learnerId, learnerId, createdAt, permissions: [...new Set(evidenceIds.flatMap((id) => evidence.find((item) => item.evidenceId === id)?.permissions ?? []))].sort(), provenance: provenance(learnerId, evidenceIds, draft.validationIds, evidence, contextDigest), manufacturedAchievements: false as const }; return { portfolioId: `PBOS-MAST-PORT-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }
