import { digestValue } from "../context";
import type { Demonstration, DemonstrationDraft, HumanValidation, MasteryEvidence } from "./contracts";
import { provenance, validatedReviewers } from "./models";
export function createDemonstrations(drafts: DemonstrationDraft[], learnerId: string, evidence: MasteryEvidence[], validations: HumanValidation[], contextDigest: string): Demonstration[] { return drafts.map((draft) => { const reviewers = validatedReviewers(draft.validationIds, validations); const body = { ...draft, evidenceIds: [...draft.evidenceIds].sort(), validationIds: [...draft.validationIds].sort(), feedback: [...draft.feedback].sort(), learnerId, reviewers, validationStatus: reviewers.length ? "VALIDATED" as const : "PENDING" as const, provenance: provenance(learnerId, draft.evidenceIds, draft.validationIds, evidence, contextDigest) }; return { demonstrationId: `PBOS-MAST-DEMO-${digestValue(body).slice(0, 16).toUpperCase()}`, ...body }; }).sort((a, b) => a.demonstrationId.localeCompare(b.demonstrationId)); }
