import { describe, expect, it } from "vitest";
import PlaybookInbox from "@/components/messages/PlaybookInbox";

describe("Playbook Inbox", () => {
  it("component is defined", () => {
    expect(PlaybookInbox).toBeTruthy();
  });
});
