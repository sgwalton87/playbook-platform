import { describe, expect, it } from "vitest";
import PlaybookHome from "@/components/home/PlaybookHome";

describe("PlaybookHome", () => {
  it("is defined", () => {
    expect(PlaybookHome).toBeTruthy();
  });
});
