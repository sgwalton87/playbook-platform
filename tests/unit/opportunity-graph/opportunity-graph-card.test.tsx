import { describe, expect, it } from "vitest";
import OpportunityGraphCard from "@/components/opportunity-graph/OpportunityGraphCard";

describe("OpportunityGraphCard", () => {
  it("is defined", () => {
    expect(OpportunityGraphCard).toBeTruthy();
  });
});
